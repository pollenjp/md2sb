# CLAUDE.md

このリポジトリで作業する Claude Code 向けの恒久的なメモ。
ユーザーから受けた指示のうち、以降のセッションでも踏襲してほしいルールをここに集約する。

## ADR (Architecture Decision Record)

Architecture の意思決定や変更が発生した場合は ADR を作成する。

### 配置とファイル名

- ディレクトリ: `docs/adr/<YYYY-MM>/`
  - 月は `TZ='Asia/Tokyo' date '+%Y-%m'` で求める。
  - 例: `docs/adr/2026-05/`
- ファイル名: `<ISO8601 timestamp>_<NN>_<task_title>.md`
  - タイムスタンプは `TZ='Asia/Tokyo' date --iso-8601=seconds` で求める (例: `2026-05-24T22:25:40+09:00`)。
  - `<NN>`: その命令で作成する ADR の連番。2 桁ゼロパディング (`01`, `02`, ...)。
    - 基本は一命令につき 1 つ。分割した方が良いと判断した場合のみ複数。
    - 連番は命令単位でリセットする (前回の命令が `_01_` で終わっていても、新しい命令の最初の ADR は `_01_` から始める)。
  - `<task_title>`: 何の決定かを簡潔に表す英小文字スネークケース。
  - 例: `2026-05-24T22:25:40+09:00_01_drop_bold_only_ordered_list_exception.md`

### 書く内容

各 ADR は以下のセクションを基本構成として含める。テンプレ的に並べるが、項目自体が不要なら無理に書かない。

1. **見出し**: `# <日付> — <決定の要約>` 形式の H1。
2. **メタ情報** (箇条書き):
   - ステータス (`Accepted` / `Proposed` / `Superseded` 等)
   - 日付 (ISO8601)
   - 関連 ADR (前の ADR を更新・撤回する場合はパスを明示)
   - 範囲 (影響を受けるファイルやモジュール)
3. **背景 (Context)**: 何が問題で、なぜこの決定が必要になったかを書く。前提となる規約・既存実装・トリガーになった要望などを引用しつつ、エッジケースが残っていたのか、ルールが不明確だったのかを明示する。
4. **決定事項 (Decisions)**: 採用したルールを箇条書きで列挙する。複数あるときは番号付きで分けると後から ADR 間で参照しやすい。各決定には、対象となる Markdown / 出力例を具体例として添える。
5. **理由 (Rationale)**: 各決定を選んだ根拠。一貫性・情報損失・実装単純化・既存挙動との整合性などの観点で説明する。
6. **影響 (Consequences)**: コード変更・テストデータ変更・利用者への影響を箇条書きで。回帰防止のため「変わったテストデータ」「副作用として変わった出力」も明示する。
7. **代替案 (Alternatives considered)**: 検討して却下した案と、その却下理由。「他にこの選択肢もあった」と読み手に伝えるための節。
8. **関連 ADR / フォローアップ**: 後続で残った検討事項、上書きされる予定の決定、後の ADR から参照されることを期待するメモなど。

### 運用上の注意

- 過去の ADR は原則として書き換えず、決定を覆す場合は新しい ADR を起こして「○○ を撤回する」「○○ を更新する」と明記する (前 ADR への相互リンクを残す)。
- 言語は日本語で書く (このリポジトリのドキュメントは日本語ベース)。
- 完全一致 / 一貫性を重視する観点での議論なので、抽象論ではなく具体例 (入力 Markdown と出力 Scrapbox の対比など) を必ず含める。

## README.md

README は「この変換器の振る舞いを利用者が把握するためのリファレンス」と位置づける。
ADR で確定した原則を、利用者が読んで使える形に翻訳して反映する。

### 何を載せるか

- プロジェクト概要 (1 行 + 補足)。
- 利用方法 (デプロイ URL からの使い方など、操作手順)。
- Features / 変換ルール:
  - セクション (見出し) の扱い。
  - インライン要素 (bold・italic・code span・リンク・画像・取り消し線など) の扱い。
  - リスト (順序付き / 順序なし)・引用・テーブル・コードブロックの扱い。
  - 仕切り線 (`---`) などの特殊扱い。
  - 各ルールには「Markdown 側の書き方 → 変換後の Scrapbox 表現」のセットを具体例として添える。
- 開発者向けセクション:
  - テストの実行コマンド。
  - テストデータの配置規約 (`md2sb/src/testdata/<case>/{in.md, out.txt}`)。
  - ADR の配置と運用 (`docs/adr/` ディレクトリ、命名規則、CLAUDE.md への参照)。

### 何を載せないか

- 実装詳細 (関数名・内部状態) は基本的に書かない。利用者には変換ルールだけ伝わればよい。
- 検討して却下した代替案や経緯は ADR 側にだけ書く。README は「最終的に決まったルール」のみ。
- 個人的な好み・思想の長い説明は冒頭の note にとどめ、ルール節の本文には混ぜない。

### ADR との対応

- ADR で新しい決定 / 既存決定の撤回が確定したら、README にも反映する。
- 反映するのは「ルールの記述」と「具体例」だけ。「なぜそうしたか」は ADR 側にしか書かない。
- ADR と README で食い違いが生じた場合、ADR の最新が正。README は遅延更新の余地があるので、決定後に必ず README 反映タスクをこなす。

## 命名・スタイルメモ

- ドキュメントの日本語は丁寧体ではなくですます調を使わない、説明文体に寄せる (ADR / README とも)。
- コードコメントは英語可。ドキュメント (`README.md`, `docs/adr/**`, `CLAUDE.md`) は日本語。
- 出力例には fenced code block を使い、Markdown 入力例と Scrapbox 出力例が混ざる場合は注記する。
